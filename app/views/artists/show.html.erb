<h1><%= @artist.name %></h1>
<ul>
<%= link_to "Add New Song", new_artist_song_path(@artist) %><br><br>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(@artist, song) %></li>
  <br>
  
  <% end %>
</ul>
